export interface IcardWelcomePage {
	icon: IconTypes;
	title: string;
	description: string;
}

type IconTypes = 'pi pi-desktop' | 'pi pi-database' | 'pi pi-globe' | 'pi pi-thumbs-up' | 'pi pi-shield';
